"""
forecasting/__init__.py

Forecasting Engine package.

Public surface:
    ForecastEngine        — main orchestrator (stateless)
    ForecastingService    — service wrapper (injected into QueryService)

Import from sub-packages directly for strategy/algorithm access.
"""
